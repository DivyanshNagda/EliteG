<resources>
    <!-- Base application theme -->
    <style name="Theme.AppCompat.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/impureWhite</item>
        <item name="android:navigationBarColor">@color/backgroundTopStart</item>
        <item name="android:statusBarColor">@color/backgroundTopStart</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        
        <!-- Material Design 3 colors -->
        <item name="colorSurface">@color/impureWhite</item>
        <item name="colorOnSurface">@color/textColor</item>
        <item name="colorSecondary">@color/colorAccent</item>
        
        <!-- Enhanced UI elements -->
        <item name="android:textColorPrimary">@color/textColor</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Splash screen theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:navigationBarColor">@color/backgroundTopStart</item>
        <item name="android:statusBarColor">@color/backgroundTopStart</item>
    </style>

    <!-- Window animation style -->
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!-- Button styles -->
    <style name="EliteGButton" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/rounded_corners</item>
        <item name="android:padding">12dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/nunito_semi_bold</item>
        <item name="android:elevation">4dp</item>
    </style>

    <!-- Primary button style -->
    <style name="EliteGButton.Primary">
        <item name="android:backgroundTint">@color/colorPrimary</item>
    </style>

    <!-- Secondary button style -->
    <style name="EliteGButton.Secondary">
        <item name="android:backgroundTint">@color/colorAccent</item>
    </style>

    <!-- Text styles -->
    <style name="EliteGText" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/nunito_light</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="EliteGText.Title">
        <item name="android:fontFamily">@font/nunito_bold</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="EliteGText.Subtitle">
        <item name="android:fontFamily">@font/nunito_semi_bold</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/textColorSecondary</item>
    </style>

    <style name="EliteGText.Body">
        <item name="android:fontFamily">@font/nunito_light</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <!-- Card style -->
    <style name="EliteGCard" parent="CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardBackgroundColor">@android:color/white</item>
        <item name="contentPadding">16dp</item>
    </style>

</resources>